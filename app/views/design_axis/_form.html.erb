<%= form_for(design_axi) do |f| %>
  <% if design_axi.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(design_axi.errors.count, "error") %> prohibited this design_axi from being saved:</h2>

      <ul>
      <% design_axi.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :parameter_id %>
    <%= f.text_field :parameter_id %>
  </div>

  <div class="field">
    <%= f.label :version_id %>
    <%= f.text_field :version_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
